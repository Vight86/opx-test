import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
  main: {
    width: '96%',
    maxWidth: '1200px',
    margin: '15px auto',
  },
  header: {
    width: '100%',
      height: '100px',
      background: '#1679df'
  }
});

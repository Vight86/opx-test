import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
  primary: {
    background: '#89e9ff',
    border: '2px solid #02b9e3',
    borderRadius: '5px',
    padding: '5px 15px',
    cursor: 'pointer'
  }
});